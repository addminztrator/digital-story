create table users (id UUID PRIMARY KEY, username TEXT, password TEXT);
create table posts (id UUID PRIMARY KEY, username text, title text, preview TEXT, content text, created TIMESTAMP NOT NULL DEFAULT NOW());